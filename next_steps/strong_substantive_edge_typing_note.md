# Strong Substantive Edge Typing Note

## Scope

This note reviews:

- `outputs/paper/33_policy_edge_typing_review/strong_substantive_edge_typed.csv`

The goal is to see what edge-typing need remains after the reviewed design-family integrations landed in the ontology-vNext builder.

## Main result

Almost all of the earlier design-family inference queue is gone.

Current counts are:

- `strong_substantive_rows`: `1`
- `relation_semantics_review`: `1`
- `design_family_inference`: `0`

The remaining row is:

- `inflation -> unemployment`

## What this means

The previous `13`-row design-family inference queue has now been absorbed into the internal evidence layer.

That is the main success condition for this pass.

The only remaining strong-substantive row no longer wants design-family recovery.
It wants a narrower review of relation semantics because it still presents more as an association / macro relationship surface than as a clean effect object.

## Current judgment

The next bottleneck is no longer design-family recovery for this queue.

It is:

- a small relation-semantics review problem for one residual row

So the internal edge-typing work did what it was supposed to do.

## Recommendation

Do not continue investing in a broad strong-substantive design-family inference workflow right now.

Do:

- keep the resolved reviewed overrides in place
- treat `inflation -> unemployment` as a separate semantic-review edge
- revisit this queue only if new strong substantive unresolved rows reappear later
