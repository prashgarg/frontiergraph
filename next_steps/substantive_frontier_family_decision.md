# Substantive Frontier Family Decision

## Decision

`not warranted yet`

## Rule applied

A dedicated substantive frontier-object family is warranted only if the refreshed strong-substantive layer still contains at least `5` rows labeled `direct_substantive_review`, with no remaining `design_family_inference` need for those rows.

## Current counts

From the refreshed review chain:

- `strong_substantive_rows`: `0`
- `direct_substantive_review`: `0`
- `design_family_inference`: `0`

## Why the decision is no

The earlier strong-substantive queue has now fully disappeared from the unresolved review surface.

That happened in two steps:

1. reviewed design-family overrides removed the bulk of the queue
2. the last residual row, `inflation -> unemployment`, moved into a reviewed relation-semantics table

So the earlier queue was mainly:

- a real internal edge-typing problem

not:

- a stable new surfaced object family waiting to be promoted

## Interpretation

This is the cleanest possible resolution of that question.

The ontology-vNext internal work did not merely rename a queue.
It actually absorbed it.

That means the right project move now is:

- freeze ontology-vNext except for future reviewed override tables
- stop trying to manufacture a new substantive frontier family from this evidence
- shift attention back to surfaced shortlist quality and paper consolidation

## What this implies next

Do:

1. keep the reviewed design-family overrides in place
2. keep the reviewed regime semantics and guardrail in place
3. keep the reviewed relation-semantics table in place
4. treat ontology-vNext as an internal semantic support layer, not the paper’s main object

Do not:

- create a new substantive frontier-object family from the current state
