# Next Steps

## Main Priority
The next real work is on `v3_paper`, not on `main`.

`main` is in good public shape. The paper, website, downloads, and seminar deck are already current. Future work should mostly be research extension rather than more cleanup on the public line.

## Highest-Priority Tasks

### 1. Start the next paper cycle on `v3_paper`
- switch to `v3_paper`
- restate the first package clearly before editing
- treat `main` as reference, not as the working branch

### 2. Resolve the path-length package
- run the `path-to-direct` path-length axis for `max_path_len = 3, 4, 5`
- decide whether a longer path length materially improves the object
- refresh the paired-family usefulness view after that choice

### 3. Resume context-extension work
- continue the context-extension paper and descriptives on `v3_paper`
- keep it off `main` until it becomes coherent enough to stand on its own

### 4. Carry forward the strongest off-paper extensions
- broader reading-budget and current-frontier extensions
- concentration / diversification follow-ons
- adopter-profile and bundle-uptake extensions if they become paper-worthy

## Secondary Tasks

### 5. Maintain `main` only when needed
- fix bugs
- refresh a public artifact if necessary
- avoid mixing new method growth into `main`

### 6. Rescue useful material from the archive only if needed
- if something valuable still lives only on `codex-post-seminar-cleanup-2026-04-13`, move it deliberately
- do not merge that branch wholesale

## Lower-Priority / Later

### 7. Broader ontology redesign
- not the immediate blocker
- current `v2.3` frozen baseline is the accepted baseline for the next rebuild

### 8. More public UX work
- only if a concrete problem appears
- not the main project bottleneck now

## Current Resume Order
If a new thread starts and there is no urgent bug:

1. read [LATEST.md](/Users/prashgarg/Library/CloudStorage/Dropbox-PrashantGarg/Prashant%20Garg/GraphDir/agents/LATEST.md)
2. read [FULL_HANDOFF_2026-04-14.md](/Users/prashgarg/Library/CloudStorage/Dropbox-PrashantGarg/Prashant%20Garg/GraphDir/agents/FULL_HANDOFF_2026-04-14.md)
3. switch to `v3_paper`
4. choose the first concrete package
5. do not reopen `main` unless the task is maintenance
