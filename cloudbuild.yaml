steps:
  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: bash
    args:
      - -c
      - |
        echo "The legacy Streamlit app is deprecated and no longer deployed from cloudbuild.yaml."
        echo "The code is preserved in app/ for archival use and possible later revival."

options:
  logging: CLOUD_LOGGING_ONLY

timeout: 600s
